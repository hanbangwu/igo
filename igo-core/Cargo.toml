[package]
name = "igo-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rules engine for the game of Go. Pure Rust, no I/O, wasm-compatible."

[dependencies]
serde = { workspace = true }
thiserror = "2.0.19"

[dev-dependencies]
serde_json = { workspace = true }
